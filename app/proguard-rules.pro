# IQ Lab keeps release rules intentionally small until external SDKs are added.
# Compose and AndroidX rules are provided by their libraries.

-keep class com.indianservers.iqlabs.MainActivity { *; }
